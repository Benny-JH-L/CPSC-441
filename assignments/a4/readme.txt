
Please ensure that the following libraries are installed:
"networkx"
"matplotlib.pyplot"

If they are not installed run:
`pip install networkx matplotlib`
in the command line to install the needed libraries for graph representation.


